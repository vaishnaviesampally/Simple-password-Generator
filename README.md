🔐 Simple Password Generator

A lightweight Python-based password generator that creates secure, random passwords.
You can easily customize the password length and choose to include uppercase letters, lowercase letters, digits, and symbols.

📘 Features

✅ Generate strong and random passwords
✅ Customize length and character types
✅ Simple and clean user interface (command-line based)
✅ Uses Python’s secrets module for cryptographic security
✅ Optional clipboard copy support

⚙️ Requirements

Python 3.7 or above

Optional modules:

pyperclip   # for clipboard copy feature
pytest      # for testing


Install them (if needed) using:

pip install -r requirements.txt
